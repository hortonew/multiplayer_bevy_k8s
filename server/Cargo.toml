[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.15", default-features = false, features = ["bevy_color"] }
bevy_renet = "1.0"
bincode = "1.3"
palette = "0.7.6"
serde = { version = "1", features = ["derive"] }
